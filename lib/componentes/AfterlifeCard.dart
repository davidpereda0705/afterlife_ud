import 'package:flutter/material.dart';

class Afterlifecard extends StatelessWidget {
  const Afterlifecard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}